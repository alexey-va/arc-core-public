schema = 1
platform = "velocity"
core_version = "2.1.1"

# Keep only capabilities the plugin actually owns. Add a capability before
# adding its infrastructure; the verifier will require the matching module and
# canonical API evidence.
capabilities = [
  "health",
  "logging",
  "runtime",
]
